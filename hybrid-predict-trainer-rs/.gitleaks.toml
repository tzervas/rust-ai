title = "hybrid-predict-trainer-rs gitleaks config"

[extend]
# Use the default gitleaks rules as a base
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
    '''\.lock$''',
    '''target/''',
    '''\.git/''',
]
